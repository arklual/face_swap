# Optional outbound proxy for the ComfyUI container (runtime).
# Put real values into root `.env` (it is ignored by git).
#
# Recommended format (DNS through proxy):
#   COMFY_PROXY=socks5h://user:pass@host:port
#
# If your password contains special characters, URL-encode them.
COMFY_PROXY=

# Enable/disable proxy usage inside the ComfyUI container without deleting COMFY_PROXY.
# 1 = enabled (default), 0 = disabled.
COMFY_PROXY_ENABLED=1

# Optional proxy for ComfyUI image build steps (apt/git/pip).
# NOTE: `apt` does NOT support SOCKS proxies, so if you only have socks5 - leave this empty,
# and keep only COMFY_PROXY for runtime.
COMFY_BUILD_PROXY=

# Comma-separated list of hosts that should bypass proxy inside the containers.
# Include docker-compose service names to avoid accidentally proxying internal traffic.
COMFY_NO_PROXY=localhost,127.0.0.1,db,redis,web,celery_worker,comfyui,huggingface.co,cas-bridge.xethub.hf.co,cdn-lfs.huggingface.co

# Frontend API base URL (used during build time)
# This should be the URL that the browser will use to access the backend API
# For production, set this to your server's public URL (e.g., https://api.yourdomain.com)
# For local development, use http://localhost:8000
VITE_API_BASE_URL=http://vm-1308.user-project-714.cloud.intcld.ru:8000

